import { ControlDecimalNumberDirective } from './control-decimal-number.directive';

describe('ControlDecimalNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlDecimalNumberDirective();
    expect(directive).toBeTruthy();
  });
});
