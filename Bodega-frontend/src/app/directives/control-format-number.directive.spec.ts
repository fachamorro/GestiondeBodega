import { ControlFormatNumberDirective } from './control-format-number.directive';

describe('ControlFormatNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlFormatNumberDirective();
    expect(directive).toBeTruthy();
  });
});
